# CLAUDE.md

홍승재의 블로그(Jekyll + Chirpy, 한국어). 글은 `_posts/`에 발행한다.

용어는 [CONTEXT.md](./CONTEXT.md)를 따른다. 특히 `읽을거리` / `글감 노트` / `주간 이슈` 세 이슈의 역할은 섞지 않는다.

## 주간 글쓰기 루틴

**목적은 뇌회로 뚫기다. 성공 지표는 글의 품질이 아니라 연속성이다.**
주간 발행은 심사가 아니라 자동 배출구다. 퀄리티가 떨어져도 발행한다.

| 요일 | 할 일 | 위치 |
|---|---|---|
| 월~목 | 하위주제 1개 → 한 문단 (음슴체 OK, 메모 먼저) | 주간 이슈 코멘트 |
| 금 | 333 얼개 + 초고. **보강 조사 무제한** | `_posts/`에 그 주 일요일 날짜로 생성 |
| 토·일 | 문장 다듬기 → 일요일 밤 발행 + 다음 주 세팅 | `_posts/`, 새 주간 이슈 |

`future: false`(Jekyll 기본값)이므로 일요일 날짜 파일은 일요일까지 빌드에서 제외된다. 금요일에 만들어 주말 내내 고쳐도 공개되지 않는다.

### 규칙

- **무조사 원칙** — 월~목 문단은 머릿속에 있는 것만으로 10~15분.
- **조사 게이트** — 10분 버틴 뒤에만 자료 1개(15분). 하루 총 30분 상한. 조사한 날은 문단 끝에 `[조사: 링크]` 표시.
- **다운시프트** — 못 쓰는 날은 한 줄만 쓰고 끝. 연속 기록 유지.
- **복구 없음** — 몰아쓰기 금지. 그 주는 있는 재료로만 조립한다.
- **읽었으면 3줄** — 자료를 읽으면 `읽을거리` 이슈에 3줄 메모를 남긴다. 이게 월~목에 인출할 재고가 된다.
- **안전선** — 회사(Creverse) 내부 구체(지표명·수치·아키텍처)는 **작성 시점에** 배제한다. 공개 리포이므로 이슈 코멘트도 즉시 공개된다. 경험은 익명화된 예로만.

### 발행 형식

- 파일명: 영문 슬러그 (`2026-08-16-why-llm-eval.md`)
- `categories`: 주제대로 (`[평가]`, `[기록]` 등) — 리듬 기준으로 분류하지 않는다
- `tags`: `주간글`을 반드시 포함
- 제목에 회차 표기 없음
- `date`: 그 주 일요일 (`2026-08-16 21:00:00 +0900`)

## Claude의 금지선

**본문 문장을 생성하지 않는다.** 뚫려야 할 회로는 승재 님의 것이고, 내가 쓴 문장이 섞이면 성장 회고 자체가 무의미해진다.

허용: 질문 던지기, 구조 지적(얼개 검증), 기계적 교정(맞춤법·주술 불일치·긴 문장 자르기), 발행 잡일(front matter·파일명·날짜·이슈 정리).

금지: 문단·초고·윤문 문장 쓰기. "이렇게 쓰면 어떨까요" 하며 예시 문장을 제시하는 것도 포함한다.

**하위주제 게이트** — 하위주제를 뽑을 때, 승재 님이 **스스로 2개 이상 적기 전에는 후보를 내지 않는다.** 그전에는 구상 유형(나열형/인과형/예시형/이론형)만 제시해 유도한다. 2개를 적고도 막히면 그때 후보 제시 + `읽을거리`에서 관련 재료를 연결한다.
얼개짜기는 이 훈련의 핵심 근육이다. 후보를 먼저 던지면 고르기만 하게 되어 그 근육을 영영 안 쓴다.

**10분 타이머는 내가 강제할 수 없다.** 관찰이 불가능하므로 원칙을 알리고 물어보기만 한다. 지켜진 척 하지 않는다.

## 명령어

`/글쓰기` — 오늘 요일에 맞는 단계로 분기한다.
